import { type PathStructure } from "./types.js";

const path: PathStructure = {
  root: "/",
  pingController: "/ping",
  login: "/login",
  user: "/user",
  teams: "/teams",
};

export default path;
